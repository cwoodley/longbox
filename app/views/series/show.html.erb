<h1>
  <%= @series.title %>
  <% if @series.pull? %>
  <!-- <span class="label label-success"><i class="icon-bookmark icon-white"></i></span> -->
  <% end -%>
</h1>

  <% if @series.issues.any? %>
    <ol class="thumbnails series-issues">
      <%= render @series.issues %>
    </ol>
  <% end %>

<div class="btn-group">
  <%= link_to 'Edit This Series', edit_series_path(@series), :class => "btn" %>
  <%= link_to 'Add New Issue', new_series_issue_path(@series), :class => "btn" %>
</div>