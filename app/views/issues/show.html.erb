<p id="notice"><%= notice %></p>
	<%=	image_tag @issue.cover.url(:medium).to_s %>
<p>
  <b>Issue number:</b>
  <%= @issue.issue_number %>
</p>

<p>
  <b>Series:</b>
  <%= @issue.series.title %>
</p>

<p>
  <b>Own:</b>
  <%= @issue.own %>
</p>


<%= link_to 'Edit', edit_series_issue_path(@issue.series.slug,@issue.issue_number) %> |
<%= link_to 'Back', series_path(@issue.series.slug) %>
